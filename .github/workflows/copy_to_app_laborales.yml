name: "[DEPRECATED] Copy new manual YAML to aplicaciones_laborales"

# DEPRECATED: This workflow is no longer needed and will never be triggered
# The centralized workflow in process_vacancies.yml handles everything in one repo
# This file is kept for reference only

on:
  push:
    paths:
      - 'NEVER_TRIGGER_THIS_WORKFLOW/*.yaml'

jobs:
  copy_yaml:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow deprecated
        run: |
          echo "⚠️  This workflow is deprecated and should never run."
          echo "Please use the new centralized workflow: .github/workflows/process_vacancies.yml"
          echo "All vacancy processing is now handled within this repository."
          exit 0
